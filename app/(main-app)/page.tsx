export const dynamic = 'force-dynamic';
import { CamearViewContainer } from '../components/CameraViewContainer';

export default function Home() {
    return (
        <CamearViewContainer />
        )
    }
    